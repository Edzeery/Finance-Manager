# Subscription System — Finance Manager

> **العربية:** سيناريو الاشتراكات — الدورة الكاملة  
> آخر تحديث: 2026-08-08

---

## Subscription Plans

| الخطة | Plan | السعر | النوع |
|-------|------|-------|-------|
| Personal | `personal` | مجاني (Free) | is_free |
| Business | `business` | $9/شهر or $90/سنة | مدفوع |
| Professional | `professional` | $19/شهر or $190/سنة | مدفوع |
| Enterprise | `enterprise` | مخصص (Custom) | مدفوع (غير عام) |

كل خطة تحتوي على `max_users`, `max_workspaces`, و `limits` (JSON) للتحكم بعدد السجلات المسموح بها شهرياً. الميزات (features) مخزّنة عبر جدول pivot `plan_plan_feature` وليست JSON.

### Billing Tables

| جدول | Key Columns | Notes |
|------|------------|-------|
| `subscription_plans` | id, name, slug, monthly_price, yearly_discount_percent, currency, is_free, is_active, is_public, max_users, max_workspaces, limits (JSON), sort_order, button_text, button_link | 4 seed plans. الميزات عبر pivot `plan_plan_feature`. |
| `subscriptions` | id, workspace_id, subscription_plan_id, status, starts_at, ends_at, trial_ends_at, canceled_at, payment_method, auto_renew, plan_price_amount | SoftDeletes |
| `plan_prices` | plan_id, billing_period (monthly/yearly), currency, price, is_active — unique on `(plan_id, billing_period, currency)` | Multi-currency أسعار منفصلة لكل عملة/فترة |
| `invoices` | id, workspace_id, subscription_id, number, status, subtotal, discount, tax, total, billing_period | Auto-numbered (INV-YYYY-XXXXX) |
| `payments` | id, workspace_id, subscription_id, amount, method, status, reference | Tracks all payment attempts |
| `coupons` | id, code, type (percentage/fixed), value, max_uses, min_amount, applies_to | Discount system |
| `tax_rates` | id, country, name, rate, type (inclusive/exclusive), is_active | Per-country taxation |

### Multi-Currency Pricing (`plan_prices`)

منذ 2026-08-08، الأسعار متعددة العملات مخزّنة في جدول `plan_prices` (سجل لكل `plan_id + billing_period + currency`). قرار معتمد من المنتج: **التسعير السنوي المستقل لكل عملة مقصود وليس تراجعاً**، ويلغي القرار السابق القائل إن السعر السنوي يجب أن يُحتسب دائماً من الصيغة.

ترتيب دقة السعر في `SubscriptionPlan::getYearlyPriceAttribute()` (`app/Models/SubscriptionPlan.php:129-138`):

1. **سجل `PlanPrice`** بصيغة `billing_period = 'yearly'` إن وُجد للخطة (سعر سنوي مستقل/خاص بالعملة).
2. **الاحتياط:** `monthly_price × 12 × (1 − yearly_discount_percent / 100)` — الصيغة الأصلية، محفوظة كاحتياط فقط.

> السعر يُجمَّد وقت إنشاء الاشتراك في `subscriptions.plan_price_amount` (Snapshot) — تعديل السعر لاحقاً لا يؤثر على الاشتراكات القائمة.

### Status States

```
past_due → active → [grace period via grace_ends_at, status stays active] → expired
              ↓                    ↓
          canceled          canceled (at period end)
trialing → active / expired
```

---

## السيناريو الكامل (بالعربية)

## فهرس المحتويات
1. [المبادئ العامة](#1-المبادئ-العامة)
2. [نظام الفوترة والرسوم](#2-نظام-الفوترة-والرسوم)
3. [نظام الكوبونات (التخفيضات)](#3-نظام-الكوبونات-التخفيضات)
4. [الفحص العام للدفعات المعلقة (hasPendingPayment)](#4-الفحص-العام-للدفعات-المعلقة-haspendingpayment)
5. [السيناريو 1: تسجيل حساب جديد + خطة مدفوعة](#5-السيناريو-1-تسجيل-حساب-جديد--خطة-مدفوعة)
6. [السيناريو 2: ترقية حساب قائم (upgrade)](#6-السيناريو-2-ترقية-حساب-قائم-upgrade)
7. [السيناريو 3: تجديد الاشتراك (renewal)](#7-السيناريو-3-تجديد-الاشتراك-renewal)
8. [السيناريو 4: إلغاء الاشتراك (cancellation / downgrade)](#8-السيناريو-4-إلغاء-الاشتراك-cancellation--downgrade)
9. [جدول ملخص البراتا والرسوم](#9-جدول-ملخص-البراتا-والرسوم)
10. [هيكلة سجلات الاشتراك — أمثلة رقمية](#10-هيكلة-سجلات-الاشتراك--أمثلة-رقمية)
11. [معايير القبول (Acceptance Criteria)](#11-معايير-القبول-acceptance-criteria)

---

## 1. المبادئ العامة

### 1.1 الاشتراك الحالي لا يُلغى حتى ينجح الدفع الجديد
في حالة الترقية، يبقى الاشتراك القديم `active` حتى يتم تأكيد نجاح الدفع للجديد. إذا فشل الدفع أو ألغاه المستخدم، يبقى الاشتراك القديم كما هو دون أي تغيير.

### 1.2 البراتا (Proration)
البراتا هو رصيد يُحتسب من الفترة المتبقية من الاشتراك الحالي عند الترقية إلى خطة أعلى. يُحتسب فقط إذا كان المستخدم في **أول 7 أيام** من الفترة الحالية.

**صيغة البراتا:**
```
الرصيد المتبقي = (الأيام المتبقية ÷ إجمالي أيام الفترة) × السعر المدفوع للخطة الحالية
```

**مثال:** خطة Business شهرياً $30، دفع قبل 5 أيام (تبقى 25 يوماً):
```
الرصيد المتبقي = (25 ÷ 30) × 30 = $25
```

### 1.3 نافذة البراتا (7 أيام) — ثابتة لجميع أنواع الخطط
نافذة الـ 7 أيام **ثابتة** (غير نسبية) لكافة أنواع الخطط، شهرية كانت أم سنوية. لا يتم تحويلها إلى نسبة مئوية.

- **لماذا 7 أيام؟** لأنها نافذة زمنية مقصودة تسمح للمستخدم باسترداد قيمة فترته المتبقية عند الترقية المبكرة، وتمنع إساءة الاستخدام في حال مرور أكثر من أسبوع على بدء الفترة.
- الرقم 7 قابل للتعديل من قاعدة البيانات (جدول `settings`) لتتمكن إدارة المنصة من تغييره دون تعديل كود.

### 1.4 فترة السماح (Grace Period)
بعد انتهاء الفترة المدفوعة، يدخل الاشتراك في حالة `past_due` لمدة 3 أيام (فترة سماح، قابلة للتعديل عبر `GRACE_PERIOD_DAYS` في `.env`). يمكن للمستخدم الدفع خلالها لتفعيل الاشتراك من جديد. بعد انتهاء فترة السماح، ينتقل الاشتراك إلى `expired`.

### 1.5 ترتيب العمليات الحسابية (Order of Operations)
عند احتساب المبلغ النهائي المطلوب دفعه، يُطبّق الترتيب التالي:

```
1. سعر الخطة (Plan Price)
2. - خصم الكوبون (Coupon Discount)         ← يُطبّق على سعر الخطة الكامل
3. = السعر بعد الخصم (After Discount)
4. - رصيد البراتا (Proration Credit)        ← فقط في الترقية خلال 7 أيام
5. = المبلغ الخاضع للرسوم (Chargeable Amount)
6. + رسم بوابة الدفع (Gateway Fee)          ← نسبة/قيمة من المبلغ الخاضع للرسوم
7. + الضريبة المضافة (Tax Added)            ← نسبة/قيمة من المبلغ الخاضع للرسوم
8. = المبلغ الإجمالي المطلوب (Final Charge)
```

> **ملاحظة:** ضريبة الإفصاح (Tax Disclosed) تُحتسب ولكنها **لا تُضاف أبداً** إلى المبلغ الإجمالي المطلوب. تُعرض فقط في الفاتورة كمعلومة.

---

## 2. نظام الفوترة والرسوم

### 2.1 جدول الربط (payment_method_tax_rate)
يربط طرق الدفع بمعدلات الضريبة/الرسوم مع تحديد نوع الشحن (`charge_type`):

| الحقل | الوصف |
|-------|-------|
| `payment_method_id` | طريقة الدفع (Foreign Key) |
| `tax_rate_id` | معدل الضريبة/الرسم (Foreign Key) |
| `charge_type` | نوع الشحن: `gateway_fee`, `tax_added`, `tax_disclosed` |

### 2.2 أنواع الشحن (Charge Types)

| النوع | السلوك | يضاف للمبلغ؟ |
|-------|--------|:----------:|
| `gateway_fee` | رسم بوابة دفع (مثل 2.5% من المبلغ) | ✅ نعم |
| `tax_added` | ضريبة تضاف إلى الفاتورة (مثل TVA 5%) | ✅ نعم |
| `tax_disclosed` | ضريبة إفصاح/زكاة تُعرض فقط في الفاتورة | ❌ لا |

### 2.3 معادلة احتساب الرسم/الضريبة
تستخدم دالة `calculateForAmount(amount)` في نموذج `TaxRate`:
- إذا كان النوع `percentage`: `الرسم = المبلغ × (المعدل ÷ 100)`
- إذا كان النوع `fixed`: `الرسم = المعدل` (قيمة ثابتة)

### 2.4 مثال عملي
طريقة دفع "Chargily" مرتبطة بمعدلين:
1. `gateway_fee` بنسبة 2.5% ← `مبلغ_الخطة_بعد_الخصم × 2.5%`
2. `tax_added` بنسبة 5% (TVA) ← `مبلغ_الخطة_بعد_الخصم × 5%`

إذا كان المبلغ الخاضع للرسوم = $100:
```
رسم البوابة = $100 × 2.5% = $2.50
الضريبة المضافة = $100 × 5% = $5.00
الإجمالي المطلوب = $100 + $2.50 + $5.00 = $107.50
ضريبة الإفصاح = تُحسب وتُعرض فقط (لا تُضاف)
```

---

## 3. نظام الكوبونات (التخفيضات)

### 3.1 أنواع الكوبونات
| النوع | الوصف | مثال |
|-------|-------|------|
| `fixed` | خصم بقيمة ثابتة بالدولار | خصم $10 |
| `percentage` | خصم بنسبة مئوية | خصم 20% |

### 3.2 قواعد تطبيق الكوبون
1. **الكوبون يُطبّق على سعر الخطة الكامل** قبل خصم البراتا.
   - مثال: خطة $50 + كوبون 10% → الخصم = $5 → السعر بعد الخصم = $45
   - ثم يُطرح رصيد البراتا إن وُجد: $45 - $25 = $20
   - ثم تُضاف الرسوم والضرائب

2. **الكوبون لا يعمل مع الترقية بعد 7 أيام** (عند عدم وجود براتا). الكوبون مخصص فقط لـ:
   - المشتركين الجدد (أول خطة)
   - الترقية المبكرة (خلال 7 أيام من بدء الفترة)

3. **قيود طرق الدفع**: إذا كان الكوبون مرتبطاً بطرق دفع محددة (جدول `coupon_payment_method`)، فلن يعمل مع أي طريقة دفع أخرى. إذا لم يكن مرتبطاً بأي طريقة، يعمل مع جميع الطرق.

4. **الشروط الإضافية**: `min_amount` (الحد الأدنى للمبلغ), `max_uses` (الحد الأقصى لمرات الاستخدام), `expires_at` (تاريخ الانتهاء).

### 3.3 مثال: كوبون + ترقية مع براتا
```
→ المستخدم على Business ($30/شهر)، مر 5 أيام (تبقى 25 يوماً)
→ يريد Enterprise ($50/شهر) + كوبون 10%

1. سعر Enterprise الكامل = $50
2. خصم الكوبون = $50 × 10% = $5
3. السعر بعد الخصم = $50 - $5 = $45
4. رصيد البراتا = (25 ÷ 30) × $30 = $25
5. المبلغ الخاضع للرسوم = $45 - $25 = $20
6. رسم البوابة (2.5%) = $20 × 2.5% = $0.50
7. الضريبة المضافة (5%) = $20 × 5% = $1.00
8. المبلغ النهائي = $20 + $0.50 + $1.00 = $21.50
```

---

## 4. الفحص العام للدفعات المعلقة (hasPendingPayment)

### 4.1 المبدأ
قبل **أي** عملية تغيير حالة الاشتراك (ترقية، تخفيض، تجديد)، يجب التحقق من عدم وجود دفعة معلقة (`pending`) لنفس المساحة (workspace). إذا وُجدت دفعة معلقة، تُمنع العملية الجديدة ويعرض للمستخدم رسالة مناسبة.

### 4.2 موقع التطبيق
يُطبّق هذا الفحص كـ **Guard** عام في الخدمة المسؤولة عن تغييرات الاشتراك، قبل الدخول في أي سيناريو:
- `upgrade` → قبل حساب البراتا وإنشاء الاشتراك الجديد
- `downgrade` → قبل تفعيل الخطة الأقل
- `renewal` → قبل محاولة تحصيل المبلغ

### 4.3 السلوك
```
إذا وُجدت دفعة pending للمساحة:
  → ارفض العملية الجديدة
  → أعد رسالة: "يوجد لديك دفعة معلقة. يرجى إكمالها قبل تغيير الاشتراك."
  → وجّه المستخدم إلى صفحة الدفع المعلق
```

> هذا الفحص يمنع إنشاء اشتراكات متعددة معلقة لنفس المساحة، وهي المشكلة التي كانت تسبب تراكم سجلات "past_due".

---

## 5. السيناريو 1: تسجيل حساب جديد + خطة مدفوعة

### 5.1 التدفق الكامل
```
1. المستخدم يسجل حساباً جديداً
2. يُوجّه إلى Onboarding → اختيار الخطة
3. يختار خطة مدفوعة (مثلاً Business شهرياً $30)
4. يدخل كود خصم (اختياري) ← يُحتسب الخصم ويعرض المبلغ
5. يظهر ملخص الدفع:
   - سعر الخطة: $30.00
   - الخصم: -$3.00 (كوبون 10%)
   - السعر بعد الخصم: $27.00
   - رسم بوابة: $0.68 (2.5%)
   - ضريبة مضافة: $1.35 (5%)
   - ضريبة إفصاح: $0.54 (2%) ← للعلم فقط، لا تضاف
   - الإجمالي المطلوب: $29.03
6. يختار طريقة الدفع
7. يضغط "اشتراك"
```

### 5.2 ماذا يفعل النظام
```
1. التحقق من صحة الكوبون مع طريقة الدفع المختارة
   - هل الكوبون ساري المفعول؟
   - هل الكوبون مرتبط بطريقة دفع محددة؟ (إن كان، هل تطابق؟)
   - هل المبلغ >= min_amount؟
2. حساب السعر:
   a = سعر الخطة (full price)
   b = خصم الكوبون (a × نسبة الخصم أو قيمة ثابتة)
   c = السعر بعد الخصم (a - b)
   d = رسوم البوابة من TaxRate (نسبة/قيمة من c حسب charge_type = gateway_fee)
   e = الضريبة المضافة من TaxRate (نسبة/قيمة من c حسب charge_type = tax_added)
   f = ضريبة الإفصاح من TaxRate (نسبة/قيمة من c حسب charge_type = tax_disclosed)
   final = c + d + e
3. إنشاء سجل اشتراك جديد بحالة past_due
4. إنشاء سجل دفع بحالة pending بالمبلغ النهائي (final)
5. توجيه المستخدم إلى بوابة الدفع
```

### 5.3 نجاح الدفع ✅
```
1. تحديث الاشتراك: past_due ← active
2. تحديث الدفع: pending ← completed
3. إنشاء فاتورة تحتوي:
   - subtotal = سعر الخطة
   - discount = خصم الكوبون
   - gateway_fee = رسم البوابة
   - tax_added = الضريبة المضافة
   - tax_disclosed = ضريبة الإفصاح
   - proration_credit = 0 (مستخدم جديد)
   - total = subtotal - discount + gateway_fee + tax_added
4. تفعيل الاشتراك: starts_at = now, ends_at = now + billing_period
```

### 5.4 فشل الدفع / إلغاء المستخدم ❌
```
1. إلغاء الاشتراك المعلق: past_due ← canceled
2. إلغاء الدفع: pending ← failed
3. إعادة توجيه المستخدم إلى صفحة إعادة المحاولة (payment-retry)
4. الخيارات المتاحة:
   - إعادة المحاولة بنفس طريقة الدفع
   - اختيار طريقة دفع أخرى
   - العودة لاختيار خطة مجانية
```

---

## 6. السيناريو 2: ترقية حساب قائم (Upgrade)

### 6.1 الشروط المسبقة
- المستخدم لديه اشتراك مدفوع فعّال (Business)
- الخطة الجديدة أعلى ترتيباً (sort_order) من الخطة الحالية
- لا توجد دفعة معلقة (`hasPendingPayment` ← ممنوع)
- لا يوجد اشتراك `past_due` سابق ← سيتم إلغاؤه إن وُجد

### 6.2 حساب البراتا
يُطبّق فقط إذا كان المستخدم في **أول 7 أيام** من الفترة الحالية.

#### الحالة أ: الترقية خلال ≤ 7 أيام
```
المستخدم: Business ($30/شهر)، مرّ 5 أيام (تبقى 25 يوماً)
الترقية إلى: Enterprise ($50/شهر)

1. سعر Enterprise الكامل = $50
2. خصم الكوبون (إن وُجد، 10%) = $50 × 10% = $5
3. السعر بعد الخصم = $50 - $5 = $45
4. رصيد البراتا:
   - الأيام المتبقية = 25
   - إجمالي الفترة = 30
   - الرصيد = (25 ÷ 30) × $30 = $25
5. المبلغ الخاضع للرسوم = $45 - $25 = $20
6. رسوم بوابة (2.5%) = $0.50 | ضريبة مضافة (5%) = $1.00
7. المبلغ النهائي = $20 + $0.50 + $1.00 = $21.50
```

#### الحالة ب: الترقية بعد 7 أيام
```
المستخدم: Business ($30/شهر)، مرّ 10 أيام (تبقى 20 يوماً)
الترقية إلى: Enterprise ($50/شهر)

1. لا يُحتسب براتا (نافذة الـ 7 أيام انتهت)
2. الكوبون لا يعمل (الكوبون فقط مع براتا أو لمشترك جديد)
3. سعر Enterprise الكامل = $50
4. رسوم بوابة (2.5%) = $1.25 | ضريبة مضافة (5%) = $2.50
5. المبلغ النهائي = $50 + $1.25 + $2.50 = $53.75
```

### 6.3 التدفق
```
1. التحقق من hasPendingPayment ← إن وُجدت، ارفض
2. إلغاء أي اشتراكات past_due سابقة للمساحة (إن وُجدت)
3. حساب البراتا (إن كان خلال 7 أيام)
4. حفظ ends_at من الاشتراك الحالي ← سنحتاجه لاحقاً
5. إنشاء سجل اشتراك جديد (للخطة الجديدة) بحالة past_due
   - starts_at = null
   - ends_at = null
   - ملاحظة: الاشتراك الحالي يبقى active (لم يُلغَ بعد)
6. إنشاء دفع pending بالمبلغ المحسوب
7. لا يتم إلغاء الاشتراك الحالي
8. توجيه المستخدم إلى بوابة الدفع
```

### 6.4 نجاح الدفع ✅
```
1. قراءة ends_at من الاشتراك القديم (Business)
2. إلغاء الاشتراك القديم: active ← canceled (مع تعيين canceled_at)
3. تحديث الاشتراك الجديد (Enterprise):
   - past_due ← active
   - starts_at = now
   - ends_at = <ends_at القديم> (نفس تاريخ نهاية فترة Business)
     (الغرض: المستخدم يدفع ثمن الأيام المتبقية فقط ولا يكتسب فترة كاملة جديدة)
4. إنشاء فاتورة بالسعر الجديد بعد الخصم والبراتا والرسوم
```

> **ملاحظة مقصودة:** مدة الاستفادة الفعلية من الخطة الجديدة قد تكون أقصر من دورة فوترة كاملة (25 يوماً في المثال بدلاً من 30). هذا سلوك مقصود — المستخدم دفع مقابل الأيام المتبقية فقط بسعر الخطة الأعلى.

### 6.5 فشل الدفع / إلغاء المستخدم ❌
```
1. إلغاء الاشتراك الجديد فقط: past_due ← canceled
2. إلغاء الدفع: pending ← failed
3. يبقى الاشتراك الحالي (Business) active كما هو دون أي تغيير
4. إعادة توجيه المستخدم إلى صفحة الاشتراكات مع رسالة فشل الدفع
5. يمكن للمستخدم إعادة المحاولة لاحقاً
```

---

## 7. السيناريو 3: تجديد الاشتراك (Renewal)

### 7.1 آلية التسعير — Snapshot (تجميد السعر)
```
عند إنشاء أي اشتراك أو إعادة تفعيله (بعد past_due):
  → يُحسب السعر من $plan->yearly_price أو $plan->monthly_price الحي لحظة العملية
  → يُخزَّن في subscriptions.plan_price_amount

عند التجديد (Auto-Renew أو Manual Renewal):
  → يُقرأ السعر من subscriptions.plan_price_amount المخزَّن فقط
  → لا يُستخدم $plan->yearly_price الحي مطلقاً
  → تعديل سعر الخطة من لوحة الإدارة لا يؤثر على الاشتراكات القائمة

التأثير:
  - المشترك الحالي يدفع نفس السعر عند التجديد الذي دفعه عند الاشتراك الأصلي
  - تعديل السعر من السوبر أدمن يؤثر فقط على الاشتراكات الجديدة من لحظة التعديل
```

### 7.2 Auto-Renew

> **ملاحظة:** نظام auto-renew مُنفّذ عبر `subscriptions:renew` artisan command المجدول يومياً في `Kernel.php`. يقرأ الاشتراكات التي `auto_renew = true` و `ends_at` قريب من الانتهاء، ويُمديد `ends_at` فوراً عند إنشاء دفعة تجديد. الفشل يُعيد الحالة إلى `past_due` مع rollback.
```
1. قبل انتهاء الفترة بـ 3 أيام → تشغيل مهمة مجدولة (Scheduled Task)
2. التحقق من hasPendingPayment ← إن وُجدت، توقف (سيُحل عند اكتمالها)
3. محاولة تحصيل المبلغ من طريقة الدفع المسجّلة
4. الحساب:
   - سعر الخطة = subscriptions.plan_price_amount (السعر المخزَّن وقت الاشتراك/آخر تجديد)
   - لا يُطبّق كوبون (الكوبون فقط للاشتراك الجديد أو الترقية المبكرة)
   - رسوم بوابة + ضريبة مضافة تُحتسب على سعر الخطة
5. نجاح الدفع ✅:
   - تمديد الاشتراك: ends_at = ends_at القديم + billing_period
   - تحديث plan_price_amount للسعر الجديد (نفس القيمة أو معاد حسابها إذا تغيرت الضرائب/الرسوم)
   - إنشاء فاتورة جديدة
6. فشل الدفع ❌:
   - الاشتراك ← past_due (فترة سماح 7 أيام)
   - إرسال إشعار للمستخدم
```

### 7.3 Manual Renewal / فترة السماح
```
1. بعد انتهاء الفترة ← الاشتراك ← past_due
2. فترة سماح 3 أيام (قابلة للتعديل عبر `GRACE_PERIOD_DAYS` في `.env`):
   - يمكن للمستخدم الدفع يدوياً
   - الحساب: subscriptions.plan_price_amount (السعر المخزَّن، لا السعر الحي)
   - تُحتسب الرسوم والضرائب من جديد (قد تتغير إذا تغيرت إعدادات TaxRate)
3. إذا دفع خلال فترة السماح ✅:
   - past_due ← active
   - ends_at = now + billing_period (فترة جديدة من تاريخ الدفع)
   - تحديث plan_price_amount للسعر الجديد
   - إنشاء فاتورة
4. إذا انتهت فترة السماح بدون دفع ❌:
   - past_due ← expired
   - لا انتقال تلقائي إلى الخطة المجانية (ليس منفّذاً بعد)
   - المساحة تصبح بدون خطة (unsubscribed state)
```

### 7.4 تخزين السعر (Snapshot) — ملفات التنفيذ
| الملف | السطر | السلوك |
|-------|-------|--------|
| `SubscriptionActivationService.php` | 100 | تخزين السعر عند إنشاء اشتراك جديد بعد نجاح الدفع |
| `SubscriptionActivationService.php` | 64 | تحديث السعر عند إعادة تفعيل اشتراك past_due |
| `OnboardingService.php` | 83 | تخزين 0 للخطط المجانية |
| `SubscriptionService.php` | 206 | تخزين السعر عند إنشاء اشتراك past_due (ترقية/تغيير) |
| `2026_07_07_000004_add_plan_price_to_subscriptions.php` | — | عمود `plan_price_amount (decimal 15,2)` في جدول `subscriptions` |

---

## 8. السيناريو 4: إلغاء الاشتراك (Cancellation / Downgrade)

### 8.1 إلغاء كلي (Cancel Subscription)
```
1. المستخدم يطلب إلغاء الاشتراك
2. التحقق من hasPendingPayment ← إن وُجدت، تنبيه (لكن قد يُسمح بالإلغاء)
   // TODO: سلوك hasPendingPayment مع الإلغاء غير محسوم — يحتاج قرار منتج
3. تحديث canceled_at للاشتراك الحالي
4. الاشتراك يبقى active حتى نهاية الفترة المدفوعة
5. في نهاية الفترة:
   - active ← canceled
   - إذا وُجدت خطة مجانية → الانتقال إليها تلقائياً
   - إذا لم توجد خطة مجانية → المساحة بدون خطة
6. يمكن للمستخدم في أي وقت قبل انتهاء الفترة إلغاء طلب الإلغاء (Reactivate)
```

### 8.2 تخفيض (Downgrade) — إلى خطة أقل
```
1. المستخدم يختار خطة أقل سعراً
2. التحقق من hasPendingPayment ← إن وُجدت، ارفض
3. TODO: التحقق من استيعاب الخطة الجديدة للاستخدام الحالي
   // TODO: سلوك فشل التحقق من الاستيعاب عند downgrade غير محسوم بعد
   // (رفض التخفيض / سماح مؤقت مع مهلة) — يحتاج قرار منتج قبل الإطلاق.
   // ممنوع تطبيق أي سلوك افتراضي (افتراضي أو تلقائي) دون الرجوع للمنتج أولاً.
4. تفعيل الخطة الجديدة فوراً (لأنها أقل سعراً)
5. لا يوجد براتا — لا يُحتسب رصيد متبقي ولا استرداد
6. إلغاء الاشتراك القديم
7. إنشاء فاتورة (بالسعر الجديد — قد تكون 0$ للخطة المجانية)
```

---

## 9. جدول ملخص البراتا والرسوم

### 9.1 متى يُحتسب البراتا؟
| الحالة | براتا؟ | ملاحظة |
|--------|:------:|--------|
| تسجيل جديد + خطة مدفوعة | ❌ | سعر الخطة كاملاً |
| ترقية خلال ≤ 7 أيام من الفترة | ✅ نعم | خصم الأيام المتبقية |
| ترقية بعد 7 أيام من الفترة | ❌ لا | سعر الخطة كاملاً بدون براتا |
| تخفيض (downgrade) | ❌ لا | خطة أقل فوراً بدون استرداد |
| تجديد (renewal) | ❌ لا | سعر الخطة كاملاً |
| تجديد بعد فترة سماح (past_due) | ❌ لا | سعر الخطة كاملاً |

### 9.2 متى يعمل الكوبون؟
| الحالة | كوبون؟ | ملاحظة |
|--------|:------:|--------|
| تسجيل جديد | ✅ نعم | يُطبّق على سعر الخطة الكامل |
| ترقية خلال ≤ 7 أيام | ✅ نعم | يُطبّق قبل خصم البراتا |
| ترقية بعد 7 أيام | ❌ لا | الكوبون لا يعمل بدون براتا |
| تخفيض (downgrade) | ❌ لا | — |
| تجديد (renewal) | ❌ لا | — |

### 9.3 مكونات الفاتورة النهائية
| المكون | يضاف للمجموع؟ | مصدره |
|--------|:-------------:|-------|
| subtotal (سعر الخطة) | أساسي | SubscriptionPlan |
| discount (خصم الكوبون) | يُطرح | Coupon |
| gateway_fee | ✅ يضاف | TaxRate (charge_type = gateway_fee) |
| tax_added | ✅ يضاف | TaxRate (charge_type = tax_added) |
| tax_disclosed | ❌ لا يضاف | TaxRate (charge_type = tax_disclosed) — للعرض فقط |
| proration_credit | يُطرح (إن وُجد) | يُحتسب من الاشتراك الحالي |
| **total** | **subtotal - discount + gateway_fee + tax_added** | |

---

## 10. هيكلة سجلات الاشتراك — أمثلة رقمية

### 10.1 ترقية (خلال 7 أيام) — نجاح الدفع
```
قبل الترقية:
  الاشتراك الحالي (Business):
    status = active
    starts_at = 2026-07-01
    ends_at   = 2026-08-01

أثناء الترقية (بانتظار الدفع):
  الاشتراك الحالي (Business):  status = active    (لم يتغير)
  الاشتراك الجديد (Enterprise): status = past_due, starts_at = null, ends_at = null

بعد نجاح الدفع ✅:
  الاشتراك الحالي (Business):
    status = canceled
    canceled_at = now
    ends_at = 2026-08-01     (نفس التاريخ — لم يتغير)

  الاشتراك الجديد (Enterprise):
    status = active
    starts_at = now           (2026-07-06 — تاريخ الدفع)
    ends_at   = 2026-08-01    (نفس ends_at القديم — يرث تاريخ النهاية)

  الفاتورة:
    subtotal          = $50.00  (سعر Enterprise)
    discount          = $5.00   (كوبون 10%)
    gateway_fee       = $0.50   (2.5% من $20)
    tax_added         = $1.00   (5% من $20)
    tax_disclosed     = $0.40   (2% من $20 — للعرض فقط)
    proration_credit  = $25.00  (رصيد Business المتبقي)
    total             = $50 - $5 + $0.50 + $1.00 = $21.50
                         (لاحظ: $21.50 = $20 + $0.50 + $1.00)
```

### 10.2 ترقية (خلال 7 أيام) — فشل الدفع
```
بعد فشل الدفع ❌:
  الاشتراك الحالي (Business):
    status = active            (لم يتغير — كما كان)
    ends_at = 2026-08-01       (لم يتغير)

  الاشتراك الجديد (Enterprise):
    status = canceled
    canceled_at = now

  الدفع:
    status = failed
```

### 10.3 تسجيل جديد — نجاح الدفع
```
بعد نجاح الدفع:
  الاشتراك الجديد:
    status = active
    starts_at = now
    ends_at   = now + billing_period (شهر/سنة)

  الفاتورة:
    subtotal          = $30.00  (Business)
    discount          = $3.00   (كوبون 10%)
    gateway_fee       = $0.68   (2.5% من $27)
    tax_added         = $1.35   (5% من $27)
    tax_disclosed     = $0.54   (2% من $27 — للعرض فقط)
    proration_credit  = $0.00   (لا يوجد براتا)
    total             = $30 - $3 + $0.68 + $1.35 = $29.03
```

---

## 11. معايير القبول (Acceptance Criteria)

- [ ] الاشتراك الحالي لا يُلغى قبل نجاح الدفع الجديد في سيناريو الترقية
- [ ] `ends_at` للاشتراك الجديد بعد الترقية = `ends_at` الأصلي للاشتراك الملغى (وليس فترة كاملة جديدة)
- [ ] الكوبون يُطبّق على سعر الخطة الكامل قبل خصم البراتا
- [ ] الكوبون لا يعمل مع الترقية بعد 7 أيام (بدون براتا)
- [ ] نافذة البراتا (7 أيام) ثابتة لكل الخطط (شهرية/سنوية) وقابلة للتعديل من قاعدة البيانات
- [ ] TODO واضح في الكود والتوثيق لسلوك فشل تحقق الاستيعاب عند downgrade، بدون تطبيق أي سلوك افتراضي
- [ ] `hasPendingPayment` يُفحص قبل upgrade وdowngrade وrenewal (وليس upgrade فقط)
- [ ] `tax_disclosed` لا يُضاف أبداً إلى total المطلوب دفعه
- [ ] `gateway_fee` و `tax_added` يُضافان إلى total
- [ ] `proration_credit` يُوثّق في الفاتورة كمبلغ خصم منفصل
- [ ] يمكن التحقق من كل نقطة أعلاه عبر اختبار (test)
